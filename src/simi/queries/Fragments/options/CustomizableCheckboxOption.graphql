fragment CustomizableCheckboxOption on CustomizableCheckboxOption {
    checkbox_value: value {
        option_type_id
        price_type
        price
        sku
        sort_order
        title
    }
}
